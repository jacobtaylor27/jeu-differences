import { Injectable } from '@angular/core';

@Injectable({
    providedIn: 'root',
})
export class CluesService {
    foo: boolean = true;

    // access for all differences
    // choose a random game between them and knowing which one it is. (cote server);
    // knowing in which quadrant a game is +  whats the difference
    // coloring in grey for few seconds the rest of the difference images ;

    // socket event clue(sending clue,gameId)
    // socket event . send (clue number,gameId)
    // socket event .on (je dessine).
    // hint1 method.
    // hint2 method.
    // hint3 method.
    // acces a l'array de diffence
    // tout les differences sont dans PrivateGameInformation et les differences trouver sont dans le set.
}
