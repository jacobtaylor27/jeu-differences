import { describe } from 'mocha';

describe('BmpEncoder', () => {
    it('getBuffer() should convert a 2D array of pixels into a buffer', () => {});
    it('encode() should convert a Bmp into a .bmp file', () => {});
});
