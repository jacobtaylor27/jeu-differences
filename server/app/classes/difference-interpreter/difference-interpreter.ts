import { Bmp } from '@app/classes/bmp/bmp';

export class DifferenceInterpreter {
    static getDifference(differenceOfBmps: Bmp) {
        // detects the number of black elements than aren't connected and regroup them into difference objects
    }
}
